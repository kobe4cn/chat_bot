apiVersion: v1
kind: Service
metadata:
  name: ai-api
  namespace: ai
  labels:
    app: ai-api
spec:
  type: ClusterIP
  selector:
    app: ai-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
